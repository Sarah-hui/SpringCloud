spring:
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled:  true
        serviceId:  configServer
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/